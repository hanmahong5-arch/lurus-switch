# S2.3: Quota Widget

**Epic**: E2 — Onboarding & Dashboard
**Points**: 3
**Status**: done
**Completed**: 2026-02-27

## Story

**As a** lurus-api user,
**I want** a quota/balance widget on the dashboard,
**So that** I can see my remaining API credits without navigating elsewhere.

## Implementation

### New Files
- `frontend/src/components/DashboardQuotaWidget.tsx` — ~110 lines, 3-state widget

### Modified Files
- `frontend/src/i18n/zh.json` — added `dashboard.quota.*` keys (8 keys)
- `frontend/src/i18n/en.json` — added `dashboard.quota.*` keys (8 keys)

## Three States

1. **Not connected** (no `proxySettings.userToken`): "Connect Account" CTA → billing page
2. **Loading/Error**: Spinner or "Offline" indicator with WifiOff icon
3. **Connected + Data**: Progress bars for total usage + daily usage, color-coded (green→amber→red)

## Acceptance Criteria

- [x] Widget shown on dashboard above tool cards
- [x] Not-connected state shows CTA to billing page
- [x] Loading state shows spinner
- [x] Error/offline state shows WifiOff icon
- [x] Connected state shows total + daily usage with progress bars
- [x] Progress bar color: green (<70%), amber (70-90%), red (>90%)
- [x] "Details" link navigates to billing page
- [x] All strings i18n-ready (zh + en)

## Verification

```
npx tsc --noEmit  # PASS — no type errors
```
