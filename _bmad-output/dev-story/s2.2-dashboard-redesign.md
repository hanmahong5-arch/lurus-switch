# S2.2: Dashboard Redesign

**Epic**: E2 — Onboarding & Dashboard
**Points**: 5
**Status**: done
**Completed**: 2026-02-27
**Depends on**: S2.3 (Quota Widget), S2.4 (Health Indicators)

## Story

**As a** user,
**I want** a dashboard showing the status of all my AI CLI tools,
**So that** I can see version, health, and config validity at a glance.

## Implementation

### Modified Files
- `frontend/src/pages/DashboardPage.tsx` — complete rewrite (~320 lines)
- `frontend/src/components/ToolCard.tsx` — added health prop + i18n
- `frontend/src/i18n/zh.json` — added `dashboard.*` keys (~30 keys)
- `frontend/src/i18n/en.json` — added `dashboard.*` keys (~30 keys)

## Layout

```
┌─────────────────────────────────────────┐
│ Header: title + subtitle + Refresh btn  │ ← i18n
├─────────────────────────────────────────┤
│ [Error banner]                          │
├─────────────────────────────────────────┤
│ [DashboardQuotaWidget]                  │ ← S2.3
├─────────────────────────────────────────┤
│ Tool Cards grid (2/3/5 cols)            │
│  - Each card has health indicator       │ ← S2.4
│                                         │
│ OR Empty State:                         │
│  "No tools installed" + Wizard CTA      │ ← links to S2.1
├─────────────────────────────────────────┤
│ Bulk Actions (Install All, Update All)  │ ← i18n
├─────────────────────────────────────────┤
│ Proxy Config (collapsible <details>)    │
├─────────────────────────────────────────┤
│ Version footer                          │ ← i18n
└─────────────────────────────────────────┘
```

## Key Changes from Previous

1. All hardcoded strings → i18n keys (`dashboard.*`)
2. Quota Widget integrated above tool cards
3. Health indicators passed to ToolCard via `CheckAllToolHealth()`
4. Empty state when no tools installed → CTA to wizard
5. Uninstall modal → i18n
6. Proxy config wrapped in collapsible `<details>`
7. Refresh button moved to header area
8. Error dismiss button → i18n

## Acceptance Criteria

- [x] All UI text uses i18n (zh + en)
- [x] Quota widget rendered above tool cards
- [x] Health indicators on each ToolCard (green/yellow/red dot)
- [x] Health data fetched after tool detection via `CheckAllToolHealth()`
- [x] Empty state shown when no tools installed with "Run Setup Wizard" button
- [x] Proxy config in collapsible `<details>` element
- [x] Uninstall confirmation modal uses i18n
- [x] Version footer uses i18n with interpolation
- [x] Responsive grid: 2→3→5 columns

## Verification

```
npx tsc --noEmit         # PASS
bun run test:run          # PASS (19/19)
```
